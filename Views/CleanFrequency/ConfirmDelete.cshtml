@model CleanMe.Application.ViewModels.CleanFrequencyViewModel

@{
    ViewData["Title"] = "Confirm Delete";
}

<h2>Delete Clean Frequency</h2>

<div class="alert alert-warning">
    Are you sure you want to delete this Clean Frequency?
</div>

<div class="mb-3">
    <label class="form-label">Name</label>
    <p class="form-control-plaintext">@Model.Name</p>
</div>

<div class="mb-3">
    <label class="form-label">Code</label>
    <p class="form-control-plaintext">@Model.Code</p>
</div>

<!-- Add additional read-only fields if needed -->

<form asp-action="ConfirmDelete" method="post">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="cleanFrequencyId" />
    <button type="submit" class="btn btn-danger">Delete</button>
    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
</form>