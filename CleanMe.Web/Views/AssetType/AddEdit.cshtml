@using CleanMe.Application.ViewModels
@using CleanMe.Domain.Enums
@using CleanMe.Shared.Helpers

@model CleanMe.Application.ViewModels.AssetTypeViewModel

@{
    ViewData["Title"] = Model.assetTypeId == 0 ? "Add Asset Type" : "Edit Asset Type";
}

<h2>@ViewData["Title"]</h2>

<form asp-action="AddEdit" method="post" id="AssetTypeForm" novalidate>
    @Html.AntiForgeryToken()

    @Html.ValidationSummary(false, "", new { @class = "text-danger" })

    <input asp-for="assetTypeId" type="hidden" />
    <input asp-for="stockCodeId" type="hidden" />
    <input type="hidden" name="returnUrl" value="@ViewBag.ReturnUrl" />

    <div class="form-group">
        <label asp-for="Name" class="form-label"></label>
        <input asp-for="Name" class="form-control" required />
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="stockCodeId" class="form-label"></label>
        <select asp-for="stockCodeId" class="form-control" asp-items="Model.StockCodes"></select>
        <span asp-validation-for="stockCodeId" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label class="form-label">Is Active</label>
        <select asp-for="IsActive" class="form-control">
            <option value="true">Active</option>
            <option value="false">Inactive</option>
        </select>
    </div>

    <button type="submit" class="btn btn-primary">Save</button>
    <a href="@ViewBag.ReturnUrl" class="btn btn-secondary">Cancel</a>
    @* <a asp-action="Index" class="btn btn-secondary">Cancel</a> *@
</form>

@section Scripts {
    <script>
    </script>
}