@model CleanMe.Application.ViewModels.AssetTypeViewModel

@{
    ViewData["Title"] = "Confirm Delete";
}

<h2>Delete Asset Type</h2>

<div class="alert alert-warning">
    Are you sure you want to delete this Asset Type?
</div>

<div class="mb-3">
    <label class="form-label">Name</label>
    <p class="form-control-plaintext">@Model.Name</p>
</div>

<!-- Add additional read-only fields if needed -->

<form asp-action="ConfirmDelete" method="post">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="assetTypeId" />
    <button type="submit" class="btn btn-danger">Delete</button>
    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
</form>